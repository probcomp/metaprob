(ns metaprob.test-runner
  (:require [cljs.test :as test :include-macros true]
            [metaprob.autotrace]
            [metaprob.code-handlers]
            [metaprob.expander]))

(defn -main
  [& args]
  ;; no tests for now!
  )
